language: node_js
node_js:
  - "5"
env:
  - NODE_ENV=ci
cache:
  yarn: true
  directories:
  - node_modules
install:
  - yarn install
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
script:
  - yarn run build
  - yarn test
