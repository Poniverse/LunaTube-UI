export const STATE_PLAYING = 'playing';
export const STATE_PAUSED = 'paused';
export const STATE_LOADING = 'loading';
export const STATE_STOPPED = 'stopped';

export const SOURCE_YOUTUBE = 'youtube';
export const SOURCE_NATIVE_AUDIO = 'native-audio';
export const SOURCE_NATIVE_VIDEO = 'native-video';

export type PlayerState = 'playing' | 'paused' | 'loading' | 'stopped';
